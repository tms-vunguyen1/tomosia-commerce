# The three reference packages this service imports, pinned at the ref the decision
# record in ../CLAUDE.md names. Never an editable path: the build has to be reproducible
# from this file alone. To move the pin, change the ref in all three lines and in the
# decision record, then re-run agent/tests.
git+https://github.com/anthropics/commerce-agents@fd4d59224ab96b43c6dc6888207c67b3bd5a24cf#subdirectory=commerce-common
git+https://github.com/anthropics/commerce-agents@fd4d59224ab96b43c6dc6888207c67b3bd5a24cf#subdirectory=merchant-agent/core
git+https://github.com/anthropics/commerce-agents@fd4d59224ab96b43c6dc6888207c67b3bd5a24cf#subdirectory=merchant-agent/runtime-messages-api

# Pinned to the same versions the shopping-agent sidecar uses.
anthropic==0.122.0
fastapi==0.141.1
httpx==0.28.1
pydantic==2.13.4
python-dotenv==1.2.2
redis==5.2.1
uvicorn==0.52.3
# The container's tzdata ships current IANA zone names only; zoneinfo falls back to
# this package for legacy ones a browser can still report (Asia/Saigon, Asia/Calcutta).
tzdata==2025.2
