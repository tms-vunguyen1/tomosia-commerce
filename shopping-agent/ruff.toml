# The reference repo's lint policy, so the copied modules stay clean under it.
line-length = 100
target-version = "py311"
exclude = ["*.md", ".venv"]

[lint]
select = ["E", "F", "I", "UP", "B", "SIM", "W291", "W293"]
ignore = ["E501"]
